#ifndef SNOWFLAKE_SAMPLER_FILTER_HPP
#define SNOWFLAKE_SAMPLER_FILTER_HPP

#include "pch.hpp"

enum class SamplerFilter
{
  DEFAULT = 0,
  LINEAR  = DEFAULT,
  POINT
};

#endif //SNOWFLAKE_SAMPLER_FILTER_HPP
